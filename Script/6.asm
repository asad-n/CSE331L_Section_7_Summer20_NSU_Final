DATA SEGMENT
NUM DB ?
MSG DB 10,13,"ENTER THE INT : $"
MSG2 DB 10,13,"EVEN$"
MSG3 DB 10,13,"ODD$"
ENDS

CODE SEGMENT
  ASSUME DS:DATA CS:CODE

START:
  MOV AX,DATA
  MOV DS,AX
  
  LEA DX,MSG
  MOV AH,9
  INT 21H
  MOV AH,1
  INT 21H
  MOV NUM,AL
  
  TEST AL,1B
  JE EVEN

ODD:
  LEA DX,MSG3
  MOV AH,9
  INT 21H
  JMP EXIT

EVEN:
  LEA DX,MSG2
  MOV AH,9
  INT 21H

EXIT:
  MOV AH,4CH
  INT 21H

ENDS
END START